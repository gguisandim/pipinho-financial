import { createCipheriv, createDecipheriv, randomBytes } from "node:crypto";
function key(){const raw=process.env.GOOGLE_TOKEN_ENCRYPTION_KEY?.trim();if(!raw)throw new Error("GOOGLE_TOKEN_ENCRYPTION_KEY não configurada.");if(/^[a-f0-9]{64}$/i.test(raw))return Buffer.from(raw,"hex");const b=Buffer.from(raw,"base64");if(b.length!==32)throw new Error("GOOGLE_TOKEN_ENCRYPTION_KEY precisa ter 32 bytes.");return b;}
export function encryptSecret(v:string){const iv=randomBytes(12),c=createCipheriv("aes-256-gcm",key(),iv),enc=Buffer.concat([c.update(v,"utf8"),c.final()]),tag=c.getAuthTag();return `v1.${iv.toString("base64url")}.${tag.toString("base64url")}.${enc.toString("base64url")}`;}
export function decryptSecret(v:string){const [ver,iv,tag,enc]=v.split(".");if(ver!=="v1"||!iv||!tag||!enc)throw new Error("Credencial criptografada inválida.");const d=createDecipheriv("aes-256-gcm",key(),Buffer.from(iv,"base64url"));d.setAuthTag(Buffer.from(tag,"base64url"));return Buffer.concat([d.update(Buffer.from(enc,"base64url")),d.final()]).toString("utf8");}
